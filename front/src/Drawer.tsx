import React from 'react';

interface DrawerProps {}

const Drawer: React.FC<DrawerProps> = ({ }) => {
  return <nav style={{backgroundColor: 'red'}}>click to load metering points</nav>
}

export default Drawer;
