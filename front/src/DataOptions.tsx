import Card, { CardHeader, CardTitle } from "@ui/Card";

const DataOptions = () => {
    return <Card className="options noshadow">
        <CardHeader>
            test header
        </CardHeader>
        <CardTitle>
            test title
        </CardTitle>
    </Card>
}

export default DataOptions;
