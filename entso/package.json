{
  "name": "@epsilon/entso",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --env-file ./dev.env --watch query.mjs",
    "lint": "eslint --ext js,jsx,mjs --fix .",
    "test": "node --test",
    "test:dev": "node -r dotenv/config --test --watch ."
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0"
  }
}
