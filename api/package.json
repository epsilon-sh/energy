{
  "name": "@epsilon/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "node --env-file ./.env index.mjs",
    "dev": "node --env-file ./dev.env --env-file ./.env --watch index.mjs",
    "lint": "eslint --ext js,jsx,mjs --fix .",
    "test": "node --test test/**/*.test.mjs",
    "test:dev": "node --env-file ./test.env --test --watch test/**/*.test.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^11.1.2",
    "cors": "^2.8.5",
    "date-fns": "^4.1.0",
    "express": "^4.21.1",
    "multer": "^1.4.5-lts.1"
  }
}
