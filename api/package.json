{
  "name": "@epsilon/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "node --env-file ./.env index.mjs",
    "dev": "node --env-file ./dev.env --watch index.mjs",
    "lint": "eslint --ext js,jsx,mjs --fix .",
    "test": "node --test",
    "test:dev": "node --env-file ./test.env --test --watch ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "multer": "^1.4.5-lts.1",
    "sqlite-async": "^1.2.0"
  }
}
