{
  "name": "@epsilon/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --env-file ./dev.env --watch index.mjs",
    "lint": "eslint --ext js,jsx,mjs --fix .",
    "test": "node --test",
    "test:dev": "node -r dotenv/config --test --watch ."
  },
  "dependencies": {
    "express": "^4.21.0",
    "sqlite-async": "^1.2.0"
  }
}
